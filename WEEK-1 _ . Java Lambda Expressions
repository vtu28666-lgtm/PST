import java.io.*;
import java.util.*;
interface PerformOperation {
    boolean check(int a);
}

public class Solution {

    public static PerformOperation isOdd() {
        return n -> n % 2 != 0;
    }

    public static PerformOperation isPrime() {
        return n -> {
            if (n < 2) return false;
            for (int i = 2; i * i <= n; i++) {
                if (n % i == 0) return false;
            }
            return true;
        };
    }

    public static PerformOperation isPalindrome() {
        return n -> {
            int original = n;
            int reverse = 0;

            while (n > 0) {
                reverse = reverse * 10 + n % 10;
                n /= 10;
            }

            return original == reverse;
        };
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        PerformOperation odd = isOdd();
        PerformOperation prime = isPrime();
        PerformOperation palindrome = isPalindrome();

        int t = sc.nextInt();

        while (t-- > 0) {
            int condition = sc.nextInt();
            int number = sc.nextInt();

            if (condition == 1) {
                System.out.println(odd.check(number) ? "ODD" : "EVEN");
            } else if (condition == 2) {
                System.out.println(prime.check(number) ? "PRIME" : "COMPOSITE");
            } else if (condition == 3) {
                System.out.println(palindrome.check(number)
                        ? "PALINDROME"
                        : "NOT PALINDROME");
            }
        }

        sc.close();
    }
}




Compiler Message
Success
Input (stdin)
5
1 4
2 5
3 898
1 3
2 12
Expected Output
EVEN
PRIME
PALINDROME
ODD
COMPOSITE



Input (stdin)
6
3 344
3 454
2 68
2 67
1 12
1 3
Expected Output
NOT PALINDROME
PALINDROME
COMPOSITE
PRIME
EVEN
ODD




nput (stdin)
3
2 232
2 2
3 333
Expected Output
COMPOSITE
PRIME
PALINDROME
